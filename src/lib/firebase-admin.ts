// This file is intentionally left blank.
// All server-side Firebase Admin logic has been removed to ensure application stability.
// Client-side interactions are handled by the standard Firebase SDK in `src/lib/firebase.ts`.
// If server-side functionality is needed in the future, this file can be re-populated
// once the hosting environment's service account configuration is guaranteed.

export const auth = null;
export const db = null;
export const storage = null;
